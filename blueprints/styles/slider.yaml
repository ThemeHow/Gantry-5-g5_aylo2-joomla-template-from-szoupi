name: Slider section settings
description: Feature colors for the Aylo2 theme
type: section

form:
  fields:

    background:
      type: input.colorpicker
      label: Background
      default: "#0071bc"
    text-color:
      type: input.colorpicker
      label: Text
      default: "#e0e0e0"
    hyperlink-color:
      type: input.colorpicker
      label: Hyperlink
      default: "#ffffff"    